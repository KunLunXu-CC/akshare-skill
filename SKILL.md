---
name: akshare-skill
description: |
  查询基金持仓收益、持仓盈亏、持有收益、收益率和当日收益。用户提供基金代码或名称、每份持仓成本价和持有份额时使用，例如“查询持仓收益，基金 270023，持仓成本 4.5071，持有份额 11778.31”。也用于安装或验证 AkShare 环境。不用于查询基金披露的股票组合持仓。
---

# AkShare 工具

本技能的主要能力是根据开放式基金最新净值、用户持仓成本价和持有份额，计算持有成本、当前市值、持仓收益、收益率和当日收益；同时负责安装 AkShare。

## 安装

执行仓库提供的安装脚本：

```bash
bash scripts/install_akshare.sh
```

安装脚本会：

1. 检查 `python3` 是否可用。
2. 检查 AkShare 是否已经安装；已安装时输出版本号并跳过安装。
3. 未安装时检查 Python 的 pip 模块是否可用。
4. 安装 AkShare，并在完成后导入 AkShare 验证版本。

安装 Python 包会修改当前 Python 环境。执行前应确认用户允许在目标环境中安装依赖。

### 查询时缺少依赖

查询脚本提示未安装 AkShare 时，不得将查询任务视为已经结束，也不得改用网页数据。按以下方式恢复：

1. 告知用户当前环境缺少 AkShare，并询问是否允许安装。
2. 未获得明确同意前不得安装，也不要反复执行查询脚本。
3. 用户同意后，在本技能目录执行 `bash scripts/install_akshare.sh`。
4. 安装成功后，使用用户原先提供的基金、成本价和份额自动重试查询，无需让用户重新输入。
5. 安装失败时说明具体错误和可执行的解决办法，不得返回虚构的收益结果。

如果当前调用方式不支持继续对话，应在本次回复中明确提出安装授权问题。进程以非零状态退出只是依赖检测信号，不代表放弃用户的查询。

## 查询基金持仓收益

当请求包含“查询持仓收益”“持仓盈亏”“持有收益”“基金收益率”等意图时使用本能力。

此类请求必须调用本技能提供的脚本获取净值并完成计算。不得绕过脚本，改用网页搜索结果或仅凭用户输入自行计算。

需要从用户请求中取得以下三个值：

- 基金代码或名称
- 每份持仓成本价
- 持有份额

缺少任一值时先向用户询问，不得猜测。信息齐全后，先阅读 [基金持仓收益参考](references/fund_holding_profit.md)，然后执行：

```bash
python3 scripts/fund_holding_profit.py "<基金代码或名称>" \
  --cost-price <每份持仓成本价> \
  --shares <持有份额> \
  --json
```

读取 JSON 后用中文回答，至少包含数据日期、最新单位净值、持有成本、当前市值、持仓收益和收益率。有前一交易日净值时，同时给出日涨幅和当日收益。

只有脚本成功返回结果后，才能回答具体收益数值。缺少 AkShare 时按上述恢复流程处理；其他执行失败应说明失败原因，不得用其他来源悄悄替代。

## 能力边界

当前不提供盘中基金估值、基金披露的股票或债券组合持仓、交易建议或其他金融数据分析能力。新增能力时应按实际需求逐步扩展。
