---
name: akshare-skill
description: |
  查询单只或多只基金的持仓收益、持仓盈亏、收益率和当日收益，并以表格汇总。用户提供基金代码或名称、每份持仓成本价和持有份额时使用，例如“查询持仓收益，基金 270023，持仓成本 4.5071，持有份额 11778.31”。也用于安装或验证 AkShare 环境。不用于查询基金披露的股票组合持仓。
---

# AkShare 工具

本技能的主要能力是根据开放式基金盘中估值或盘后净值、用户持仓成本价和持有份额，计算持有成本、当前市值、持仓收益、收益率和当日收益；同时负责安装 AkShare。

## 安装

执行仓库提供的安装脚本：

```bash
bash scripts/install_akshare.sh
```

安装脚本会：

1. 检查 `python3` 是否可用。
2. 优先检查技能目录的持久化 `.venv`；AkShare 已安装时输出版本号并跳过安装。
3. 当前 Python 已经可以导入 AkShare 时直接复用。
4. 两处都不可用时，在技能目录创建 `.venv`、安装 AkShare 并验证版本。

首次创建 `.venv` 和安装 Python 包前应取得用户许可。查询脚本即使用系统 `python3` 启动，也会自动切换到技能的 `.venv`；后续会话不需要激活环境或重复安装。

### 查询时缺少依赖

查询脚本提示未安装 AkShare 时，不得将查询任务视为已经结束，也不得改用网页数据。按以下方式恢复：

1. 安装脚本和查询脚本都会先复用技能目录的 `.venv`；不得仅因系统 Python 无法导入 AkShare 就判定依赖缺失。
2. 确认技能 `.venv` 和当前 Python 都不可用后，告知用户缺少 AkShare，并询问是否允许首次安装。
3. 未获得明确同意前不得安装，也不要反复执行查询脚本。
4. 用户同意后，在本技能目录执行 `bash scripts/install_akshare.sh`。
5. 安装成功后，使用用户原先提供的基金、成本价和份额自动重试查询，无需让用户重新输入。
6. 安装失败时说明具体错误和可执行的解决办法，不得返回虚构的收益结果。

如果当前调用方式不支持继续对话，应在本次回复中明确提出安装授权问题。进程以非零状态退出只是依赖检测信号，不代表放弃用户的查询。

## 查询基金持仓收益

当请求包含“查询持仓收益”“持仓盈亏”“持有收益”“基金收益率”等意图时使用本能力。

此类请求必须调用本技能提供的脚本获取净值并完成计算。不得绕过脚本，改用网页搜索结果或仅凭用户输入自行计算。

脚本会自动判断中国内地市场阶段并选择价格：

- 交易日 09:30–15:00：有当天估值时使用盘中估值。
- 交易日 15:00 后：已公布当天净值时使用盘后净值；尚未公布时可使用当天收盘估值，但必须标注“净值待公布”。
- 盘前、周末或接口没有当天估值时：使用最新已公布净值，并在价格状态中明确说明。
- 批量净值接口的当前净值或前一净值为空时：自动读取该基金历史净值补齐；日增长率为空时根据相邻两次净值计算。

每只基金都需要从用户请求中取得以下三个值：

- 基金代码或名称
- 每份持仓成本价
- 持有份额

任一基金缺少字段时先向用户询问，不得猜测。信息齐全后，先阅读 [基金持仓收益参考](references/fund_holding_profit.md)。单只基金执行：

```bash
python3 scripts/fund_holding_profit.py "<基金代码或名称>" \
  --cost-price <每份持仓成本价> \
  --shares <持有份额> \
  --json
```

多只基金使用可重复的 `--holding "<基金代码或名称>,<成本价>,<份额>"` 参数一次查询，例如：

```bash
python3 scripts/fund_holding_profit.py \
  --holding "270023,4.5071,11778.31" \
  --holding "000002,1.0000,2000" \
  --json
```

读取 JSON 后用中文 Markdown 表格回答。每只基金占一行，表头使用“基金、日期、日涨幅、持有成本、持仓收益、收益率、当日收益”；不要展示价格状态、当前净值、前净值、持仓成本价、份额或当前市值列。表格后固定追加“合计持有成本 **¥…**，持仓收益 **¥…**，当日收益 **¥…**”。只有历史接口也无法提供前一净值时，当日收益才显示“暂无”；任一基金缺少当日收益时，合计当日收益也显示“暂无”。即使只有一只基金也使用相同格式。凡使用估值计算，可在表格外提醒最终收益以基金公司公布净值为准。

只有脚本成功返回结果后，才能回答具体收益数值。缺少 AkShare 时按上述恢复流程处理；其他执行失败应说明失败原因，不得用其他来源悄悄替代。

## 能力边界

当前不提供基金披露的股票或债券组合持仓、交易建议或其他金融数据分析能力。盘中估值仅供参考，不能视为基金公司公布的正式净值。新增能力时应按实际需求逐步扩展。
